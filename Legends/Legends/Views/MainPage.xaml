<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Legends.MainPage" BackgroundColor="#112120">

    <StackLayout>
        <Button x:Name="LegendsPage" Text="Legends" Clicked="LegendsPage_Clicked"></Button>
    </StackLayout>
    

    <!--<ListView x:Name="listviewChampions">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Value.Name}" Detail="{Binding Value.Title}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>-->

</ContentPage>
